<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "help">
	<select id="list" resultType="helpVo">
		
		select * from help where h_delyn='N' order by hidx desc
		
		
	</select>
	
	<!-- 동적 SQL문으로 처리: choose-when-otherwise 구문 -->
	<insert id="addHelp" parameterType="helpVo">
		<![CDATA[
		insert into help(
						h_userId, h_userName, h_name, h_no, h_number, h_img)
					values(#{member_id}, #{member_name}, #{h_name}, #{h_no}, #{h_number}, #{h_img})	
		]]>	
	
	
	</insert>



</mapper>